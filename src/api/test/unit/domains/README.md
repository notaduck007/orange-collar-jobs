# Unit tests for `src/domains/**`

Mirror the domain tree here. Example:

```
test/unit/domains/jobs/jobs.service.spec.ts  →  src/domains/jobs/jobs.service.ts
```

Import the unit under test via `@domains/*` path aliases.
