/**
 * Canonical Prisma client + types for runtime and compile.
 * Import from here — not `@prisma/client` (Prisma 7 custom output; tsc does not rewrite path aliases).
 */
export * from "../../generated/prisma/client.js";
